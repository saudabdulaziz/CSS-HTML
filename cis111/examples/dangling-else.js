//classic programming language problem
//which if does the one else go with?

"use strict";

var signalOn = false, color = 'amber';

if(signalOn)
if(color == 'red' || color == 'amber')
console.log('Stop');
else
console.log('Signal failure! Proceed with extreme caution!');
    
console.log('\nDrive Safely');
/* 
   exercise 1: indent this example in a way that corresponds to what it does at runtime.
   exercise 2: modify the example to do the right thing.
*/




